import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  heading: {
    color: '#9963ea',
    marginTop: 20,
    marginBottom: 20,
    fontSize: 20,
  },
  bodyText: {
    fontSize: 20,
  },
});

export default styles;
